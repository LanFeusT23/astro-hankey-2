rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /gallery/{allPaths=**} {
      // Anyone can read gallery images (public portfolio)
      allow read: if true;
      // Only authenticated users can upload or delete
      allow write: if request.auth != null;
    }
  }
}
